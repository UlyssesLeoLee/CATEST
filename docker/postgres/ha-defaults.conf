# HA baseline — applied to both primary and replica.
# On replica: standby.signal overrides behaviour; these settings are still valid.
wal_level = replica
max_wal_senders = 10
wal_keep_size = 128MB
hot_standby = on
hot_standby_feedback = on
synchronous_commit = local
