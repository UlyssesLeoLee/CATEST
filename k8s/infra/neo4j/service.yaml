apiVersion: v1
kind: Service
metadata:
  name: neo4j
  namespace: catest
spec:
  ports:
    - port: 37474
      targetPort: 7474
      name: http
    - port: 37687
      targetPort: 7687
      name: bolt
  selector:
    app: neo4j
  type: ClusterIP
