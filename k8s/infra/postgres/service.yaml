apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: catest
spec:
  ports:
    - port: 34321
      targetPort: 5432
      name: postgres
  selector:
    app: postgres
  type: ClusterIP
