apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: catest
spec:
  ports:
    - port: 9092
      targetPort: 9092
      name: internal
    - port: 39092
      targetPort: 39092
      name: external

  selector:
    app: kafka
  type: ClusterIP
