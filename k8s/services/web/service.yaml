apiVersion: v1
kind: Service
metadata:
  name: catest-web-base
  namespace: catest
spec:
  type: ClusterIP
  ports:
    - port: 33000
      targetPort: 3000
      name: http
  selector:
    app: catest-web
