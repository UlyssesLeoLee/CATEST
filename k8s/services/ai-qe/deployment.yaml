apiVersion: apps/v1
kind: Deployment
metadata:
  name: catest-ai-qe
  namespace: catest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catest-ai-qe
  template:
    metadata:
      labels:
        app: catest-ai-qe
    spec:
      serviceAccountName: catest-service-account
      containers:
        - name: ai-qe
          image: ghcr.io/ulyssesleolee/catest-ai-qe:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 34084
              name: http
          env:
            - name: CATEST_AI_SERVICE_NAME
              value: "ai-qe"
            - name: CATEST_AI_SERVICE_PORT
              value: "34084"
            - name: CATEST_AI_KAFKA_BROKER
              value: "kafka:39092"
            - name: CATEST_AI_ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: anthropic-api-key
            - name: CATEST_AI_LANGSMITH_API_KEY
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: langsmith-api-key
          resources:
            limits:
              cpu: "500m"
              memory: "512Mi"
            requests:
              cpu: "100m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 34084
            initialDelaySeconds: 10
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /healthz
              port: 34084
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: catest-ai-qe
  namespace: catest
spec:
  selector:
    app: catest-ai-qe
  ports:
    - port: 34084
      targetPort: 34084
      name: http
